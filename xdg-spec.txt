# XDG Base Directory Specification
# Installed as ~/.config/environment.d/90-fedora-bootstrap.conf
XDG_CONFIG_HOME=${XDG_CONFIG_HOME:-$HOME/.config}
XDG_DATA_HOME=${XDG_DATA_HOME:-$HOME/.local/share}
XDG_STATE_HOME=${XDG_STATE_HOME:-$HOME/.local/state}
XDG_CACHE_HOME=${XDG_CACHE_HOME:-$HOME/.cache}

# --- Dev Tools ---
CARGO_HOME=${CARGO_HOME:-$XDG_DATA_HOME/cargo}
RUSTUP_HOME=${RUSTUP_HOME:-$XDG_DATA_HOME/rustup}
GOPATH=${GOPATH:-$XDG_DATA_HOME/go}
GOMODCACHE=${GOMODCACHE:-$XDG_DATA_HOME/go/pkg/mod}
NPM_CONFIG_USERCONFIG=${NPM_CONFIG_USERCONFIG:-$XDG_CONFIG_HOME/npm/npmrc}
NODE_REPL_HISTORY=${NODE_REPL_HISTORY:-$XDG_STATE_HOME/node/repl_history}
# Python 3.13+
PYTHON_HISTORY=${PYTHON_HISTORY:-$XDG_STATE_HOME/python/history}
# Older Python workaround
PYTHONSTARTUP=${PYTHONSTARTUP:-$XDG_CONFIG_HOME/python/pythonrc}
BUNDLE_USER_CONFIG=${BUNDLE_USER_CONFIG:-$XDG_CONFIG_HOME/bundle}
BUNDLE_USER_CACHE=${BUNDLE_USER_CACHE:-$XDG_CACHE_HOME/bundle}
GEM_HOME=${GEM_HOME:-$XDG_DATA_HOME/gem}
GEM_SPEC_CACHE=${GEM_SPEC_CACHE:-$XDG_CACHE_HOME/gem}
GRADLE_USER_HOME=${GRADLE_USER_HOME:-$XDG_DATA_HOME/gradle}
NUGET_PACKAGES=${NUGET_PACKAGES:-$XDG_DATA_HOME/nuget}
STACK_ROOT=${STACK_ROOT:-$XDG_DATA_HOME/stack}

# --- Misc Apps ---
GNUPGHOME=${GNUPGHOME:-$XDG_DATA_HOME/gnupg}
WGETRC=${WGETRC:-$XDG_CONFIG_HOME/wget/wgetrc}
DOCKER_CONFIG=${DOCKER_CONFIG:-$XDG_CONFIG_HOME/docker}
MYSQL_HISTFILE=${MYSQL_HISTFILE:-$XDG_STATE_HOME/mysql/history}
PSQL_HISTORY=${PSQL_HISTORY:-$XDG_STATE_HOME/psql/history}
SQLITE_HISTORY=${SQLITE_HISTORY:-$XDG_STATE_HOME/sqlite/history}
CUDA_CACHE_PATH=${CUDA_CACHE_PATH:-$XDG_CACHE_HOME/nv}
ANSIBLE_HOME=${ANSIBLE_HOME:-$XDG_CONFIG_HOME/ansible}
W3M_DIR=${W3M_DIR:-$XDG_STATE_HOME/w3m}
